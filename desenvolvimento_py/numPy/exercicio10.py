# (Funções Universais): 
# Aplique a função de raiz quadrada (np.sqrt()) e a função exponencial (np.exp()) 
# em um array de números de 1 a 5.

import numpy as np

array = np.array([1, 2, 3, 4, 5])
raiz_quadrada = npm.sqrt(array)
exponencial = np.exp(array)

print("Array original:", array)
print("Raiz quadrada:", raiz_quadrada)
print("Exponencial:", exponencial)